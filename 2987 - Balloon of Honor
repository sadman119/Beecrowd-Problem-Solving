#include <stdio.h>

int main() {
    char ch;
    scanf("%c", &ch);

    if (ch >= 'A' && ch <= 'Z') {
        int position = ch - 'A' + 1;
        printf("%d\n", position);
    } else {
        printf("Invalid input\n");
    }

    return 0;
}
