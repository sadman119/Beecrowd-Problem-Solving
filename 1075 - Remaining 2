#include <stdio.h>

int main() {
    int i, n;
    scanf("%d", &n);

    i = 1;

    do {
        if (i % n == 2) {
            printf("%d\n", i);
        }
        i++;
    } while (i <= 10000);

    return 0;
}
