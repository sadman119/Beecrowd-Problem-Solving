#include <stdio.h>

int main() {
    int n;

    while (1) {
        if (scanf("%d", &n) == EOF)
            break;

        if (n % 6 == 0) {
            printf("Y\n");
        } else {
            printf("N\n");
        }
    }

    return 0;
}
