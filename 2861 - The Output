#include <stdio.h>
#include <string.h>

int main() {
    char sen[100];
    int cycle;

    scanf("%d", &cycle);
    
    for (int i = 1; i <= cycle; i++) {
        scanf(" %c",&sen[i]);
        printf("gzuz\n");
    }

    return 0;
}
