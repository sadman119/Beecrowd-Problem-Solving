#include <stdio.h>
#include <math.h>

int main() {
    double x, y;
    scanf("%lf %lf", &x, &y);
    
    double r = pow(x, 3);
    double a = (4.0/3.0) * (3.1415 * r);
    int num = (int)(y / a);
    
    printf("%d\n", num);

    return 0;
}
