#include <stdio.h>

int main() {
    long long int A, B, sum = 0,i;
    scanf("%lld %lld", &A, &B);

    for ( i = A; i <= B; i++) {
        sum += i;
    }

    printf("%lld\n", sum);

    return 0;
}
