#include <stdio.h>
#include <string.h>

int main() {
    int n,c;
    scanf("%d", &n);

for(int i=0; i<n; i++){
    char x[10001];
    scanf("%s", x);

    c = strlen(x);
    double z = 0.01 * c;
    printf("%.2lf\n", z);
}
    return 0;
}
